# Normalise all text files to LF in the repo; check out as-is on Windows
* text=auto eol=lf

# Windows batch files must stay CRLF
*.bat text eol=crlf

# Binaries — no line-ending conversion
*.ico binary
*.png binary
*.exe binary
*.dll binary
